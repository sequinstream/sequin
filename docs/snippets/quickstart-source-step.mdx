<Step title='Note "Source" configuration'>
    In the "Source" card, note that the `sequin-playground` database and `public` schema are pre-selected. Leave these defaults:

    <Frame>
        <img style={{ maxWidth: '500px' }} src="/images/quickstart/source-card.png" alt="Source card showing the sequin_playground database and products table" />
    </Frame>
</Step>